{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Active C# Script",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "Run Active File",
            "program": "", 
            "cwd": "${fileDirname}"
        }
    ]
}
